<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aj.androidatdd"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Navigator"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserStartActivity"></activity>
        <activity android:name=".RecipeMainScreenActivity"></activity>
        <activity android:name=".SelectRecipeActivity"></activity>
        <activity android:name=".activities.ttdo.CookingAndServingActivity"></activity>
        <activity android:name=".activities.ttdo.IngredientsActivity"></activity>
        <activity android:name=".activities.ttdo.PreparationsActivity"></activity>
    </application>

</manifest>